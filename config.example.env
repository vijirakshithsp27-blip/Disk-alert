# Copy this file to .env and edit values as needed.
THRESHOLD=80
LOG_DIR=./logs
# Example: send email using mailx (system must have mailx configured)
# ALERT_CMD="mail -s 'Disk alert' admin@example.com"
# Or send to a webhook (example with curl):
# ALERT_CMD="xargs -I {} curl -X POST -H 'Content-Type: text/plain' --data '{}' https://your-webhook.example/endpoint"
